# 导航栏下方轮播图 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 导航栏下方实现左右箭头切换、循环播放的全宽轮播图。

**Architecture:** 静态站无框架。HTML 放 `.carousel` 结构，CSS 控制轨道平移，纯 JS 监听箭头点击修改 `transform: translateX` 实现切换。3 张图来自 `img/banners/`。

**Tech Stack:** 原生 HTML/CSS/JS，无依赖。

**测试方式:** 无自动化测试框架。每步用浏览器打开 `index.html` 目视 + 控制台验证。

---

### Task 1: HTML 轮播结构

**Files:**
- Modify: `index.html`（`</nav>` 之后、`<script>` 之前）

- [ ] **Step 1: 加轮播结构 + JS 引用**

在 `index.html` 的 `</nav>` 和 `</body>` 之间插入：

```html
    <div class="carousel">
        <div class="carousel-track">
            <img src="./img/banners/banner_1.png" alt="banner 1">
            <img src="./img/banners/banner_2.png" alt="banner 2">
            <img src="./img/banners/banner_3.png" alt="banner 3">
        </div>
        <button class="carousel-arrow prev" type="button">‹</button>
        <button class="carousel-arrow next" type="button">›</button>
    </div>

    <script src="./js/index.js"></script>
```

- [ ] **Step 2: 浏览器验证结构存在**

打开 `index.html`，确认 3 张图竖直堆叠显示（样式未加，乱排正常），无报错。

---

### Task 2: CSS 轮播样式

**Files:**
- Modify: `css/index.css`（文件末尾追加）

- [ ] **Step 1: 追加轮播样式**

在 `css/index.css` 末尾追加：

```css
.carousel {
    position: relative;
    width: 100%;
    height: 400px;
    overflow: hidden;
}

.carousel-track {
    display: flex;
    height: 100%;
    transition: transform 0.4s ease;
}

.carousel-track img {
    width: 100%;
    height: 400px;
    flex-shrink: 0;
    object-fit: cover;
}

.carousel-arrow {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: rgba(0, 0, 0, 0.4);
    color: #fff;
    font-size: 24px;
    cursor: pointer;
    border: none;
    line-height: 40px;
}

.carousel-arrow.prev { left: 16px; }
.carousel-arrow.next { right: 16px; }

.carousel-arrow:hover {
    background: rgba(0, 0, 0, 0.6);
}
```

- [ ] **Step 2: 浏览器验证**

刷新，3 张图并排一行、只显示第一张，两侧有半透明圆形箭头。`body` 无横向滚动条。

---

### Task 3: JS 切换逻辑

**Files:**
- Create: `js/index.js`

- [ ] **Step 1: 写切换脚本**

创建 `js/index.js`：

```js
(function () {
    var track = document.querySelector('.carousel-track');
    var prevBtn = document.querySelector('.carousel-arrow.prev');
    var nextBtn = document.querySelector('.carousel-arrow.next');

    var count = track.children.length;
    var index = 0;

    function show(i) {
        // 循环：负数取模结果仍为负，手动转正
        index = ((i % count) + count) % count;
        track.style.transform = 'translateX(-' + (index * 100) + '%)';
    }

    nextBtn.addEventListener('click', function () {
        show(index + 1);
    });

    prevBtn.addEventListener('click', function () {
        show(index - 1);
    });
})();
```

- [ ] **Step 2: 浏览器验证**

打开 `index.html`：
- 点 `›`：依次切 2、3，第 3 张再点回到第 1 张
- 点 `‹`：反向循环，第 1 张点左键回第 3 张
- 切换有平滑过渡，图片不变形

---

### Task 4: 提交

- [ ] **Step 1: 提交**

```bash
git add index.html css/index.css js/index.js
git commit -m "导航栏下方轮播图"
```

**预期:** 3 个文件入库，spec + plan + 代码全部提交。
